name: Publish alpha.36 recovery (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'alpha.36 is already published; this workflow is archived and cannot republish it.'
